# Admin frontend modules

Phase 1.5 migration scaffold.

The current admin.js behavior remains unchanged during the first migration commit. Future commits will move API and Telegram view logic into this directory without changing backend URLs.

[project]
name = "tgdrive-proxy"
version = "0.4.0"
description = "Optional network proxy capability plugin for tgdrive"
requires-python = ">=3.11"
dependencies = []

[project.entry-points."tgdrive.plugins"]
proxy = "tgdrive_proxy:ProxyPlugin"

[tool.setuptools]
py-modules = ["tgdrive_proxy"]
